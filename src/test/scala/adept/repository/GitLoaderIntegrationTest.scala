package adept.repository

import org.scalatest.FunSuite
import org.scalatest.Matchers

//TODO: Move out of tests and into a separate folder so it is not executed all the time
class GitLoaderIntegrationTest extends FunSuite with Matchers {
  test("INTEGRATION: init, fetch, push") {
    
  }
}